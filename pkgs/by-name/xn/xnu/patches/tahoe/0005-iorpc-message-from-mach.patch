From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Theo Paris <theo@theoparis.com>
Date: Tue, 15 Sep 2026 20:21:46 -0700
Subject: [PATCH 05/11] iokit: add IORPCMessageFromMach for DriverKit iig code

Kernel-side iig-generated DriverKit code calls the Apple-named
IORPCMessageFromMach(); the fork only implemented the reply-specific
IORPCMessageFromMachReply(). Generalize it with an explicit reply flag
and expose the helper to kernel builds via IORPC.h.
---
 iokit/IOKit/IORPC.h           |  6 ++++++
 iokit/Kernel/IOUserServer.cpp | 13 +++++++++----
 2 files changed, 15 insertions(+), 4 deletions(-)

diff --git a/iokit/IOKit/IORPC.h b/iokit/IOKit/IORPC.h
index 7ffc6b73..7a3024bd 100644
--- a/iokit/IOKit/IORPC.h
+++ b/iokit/IOKit/IORPC.h
@@ -234,6 +234,12 @@ struct IORPC {
 };
 typedef struct IORPC IORPC;
 
+#ifdef KERNEL
+/* OpenDarwin/meson: kernel-side driverkit code (iig-generated) needs the
+ * Apple-named helper; the fork implements IORPCMessageFromMachReply(). */
+IORPCMessage *IORPCMessageFromMach(IORPCMessageMach * msg, bool reply);
+#endif /* KERNEL */
+
 enum {
 	kOSClassCanRemote   = 0x00000001,
 };
diff --git a/iokit/Kernel/IOUserServer.cpp b/iokit/Kernel/IOUserServer.cpp
index 14d77fb6..a96803df 100644
--- a/iokit/Kernel/IOUserServer.cpp
+++ b/iokit/Kernel/IOUserServer.cpp
@@ -2444,7 +2444,7 @@ OSMetaClassBase::Invoke(IORPC rpc)
 	IORPCMessage    * message;
 
 	assert(rpc.sendSize >= (sizeof(IORPCMessageMach) + sizeof(IORPCMessage)));
-	message = rpc.kernelContent;
+	message = IORPCMessageFromMach(rpc.message, false);
 	if (!message) {
 		return kIOReturnIPCError;
 	}
@@ -3680,15 +3680,14 @@ IOUserServer::rpc(IORPC rpc)
 
 /* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */
 
-static IORPCMessage *
-IORPCMessageFromMachReply(IORPCMessageMach * msg)
+IORPCMessage *
+IORPCMessageFromMach(IORPCMessageMach * msg, bool reply)
 {
 	mach_msg_size_t              idx, count;
 	mach_msg_port_descriptor_t * desc;
 	mach_msg_port_descriptor_t * maxDesc;
 	size_t                       size, msgsize;
 	bool                         upgrade;
-	bool                         reply = true;
 
 	msgsize = msg->msgh.msgh_size;
 	count   = msg->msgh_body.msgh_descriptor_count;
@@ -3720,6 +3719,12 @@ IORPCMessageFromMachReply(IORPCMessageMach * msg)
 	return (IORPCMessage *)(uintptr_t) desc;
 }
 
+static IORPCMessage *
+IORPCMessageFromMachReply(IORPCMessageMach * msg)
+{
+	return IORPCMessageFromMach(msg, true);
+}
+
 ipc_port_t
 IOUserServer::copySendRightForObject(OSObject * object, ipc_kobject_type_t type)
 {
